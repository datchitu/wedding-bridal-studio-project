import { styled } from "styled-components";

export const WrapperLableText = styled.a`
    cursor: pointer;
    text-decoration: none;
    font-size: 2.2rem;
    color: var(--text-color);
    font-weight: 600;
    line-height: 2.4rem;
    height: 3.6rem;
    margin: 8px 10px;
    overflow: hidden;
    display: block;
    display: -webkit-box;
    -webkit-box-orient: vertical;
    -webkit-line-clamp: 2;
`
    
export const WrapperAvatar = styled.a`
    cursor: pointer;
`